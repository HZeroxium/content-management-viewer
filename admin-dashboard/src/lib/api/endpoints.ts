// src/lib/api/endpoints.ts
export const AUTH = {
  login: "/auth/login",
  logout: "/auth/logout",
  // …
};

export const USERS = {
  list: "/users",
  // …
};

export const CONTENT = {
  list: "/content",
  upload: "/content", // etc.
};

export const FILES = {
  upload: "/files/upload",
};
